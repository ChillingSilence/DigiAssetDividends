
Install dependencies
=============================

`npm install`


Run node
=============================

`./start-node`
to run as service

or

`./start-node-interactive`
to run for debug (console log, CTRL-C will break it)
