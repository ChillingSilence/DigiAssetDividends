Install Node.js and dependencies
================================================

To install dependencies:

`apt install composer`
`composer install`

Need to install, configure and run as a service [digibyte-nownode](https://github.com/cept73/digibyte-nownode)


Tune Config.php
================================================
Copy `config.example.php` to `config.php` and tune

You should specify same port as at the digibyte-nownode app, admin address, fee in percents
